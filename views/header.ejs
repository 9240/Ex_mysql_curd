<nav class="navbar navbar-expand-lg navbar-light bg-primary fixed-top">
  <div class="container">
    <h2>9240</h2>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav mr-auto">
        <li class="nav-item active">
          <a class="nav-link" href="/">Home <span class="sr-only">(current)</span></a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="./about">About</a>
        </li>
      </ul>
      <div class="form-inline my-2 my-lg-0">
        <% if(email){ %>
          <%= email %>
          <button class="btn btn-outline-info text-white mr-1 ml-1" id="addClass">添加</button>
          <button class="btn btn-outline-info text-white" id="logout">注销</button>
        <% }else{ %>
          <a class="btn btn-outline-info mr-1 text-white" href="./login">登录</a>
          <a class="btn btn-outline-info text-white" href="./register">注册</a>
        <% } %>
        </div>
    </div>
  </div>
</nav>

<script>
  $(function(){
    $("#logout").click(function(){
      document.cookie.split("; ").forEach(item=>{
        document.cookie = item.split("=")[0] + '=;  expires=Thu, 01 Jan 1970 00:00:01 GMT;'
      })
      location.href = '/'
    })
    $("#addClass").click(function(){
      location.href = './addClass'
    })
  })
</script>
